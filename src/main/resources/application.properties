# Arquivo: application.properties (configuraÃÂ§ÃÂµes comuns)

jwt.public.key=classpath:keys/app.pub
jwt.private.key=classpath:keys/app.key

# Definir o nÃ­vel de log para toda a aplicaÃ§Ã£o
logging.level.root=DEBUG

# Aumenta o nÃ­vel de log para tomcat
logging.level.org.apache.coyote=DEBUG
logging.level.org.apache.tomcat=DEBUG
logging.level.org.apache.catalina=DEBUG

# Definir o nÃ­vel de log para um pacote especÃ­fico (ex: logs do Spring Security)
logging.level.org.springframework.security=DEBUG
# Definindo o perfil ativo baseado em variÃÂ¡vel de ambiente (padrÃÂ£o para 'dev')
spring.profiles.active=${APP_PROFILE:dev}

# ConfiguraÃÂ§ÃÂ£o do CORS (ajustar conforme necessÃÂ¡rio)
cors.origins=${CORS_ORIGINS:http://localhost:5173,http://localhost:3000}

# Outras configuraÃÂ§ÃÂµes globais
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# Desabilitar as migrações do Flyway (não será executado)
#flyway.enabled=false

spring.flyway.locations=classpath:db/migration

# Arquivo: application.properties (configuraÃ§Ãµes comuns)

jwt.public.key=classpath:keys/app.pub
jwt.private.key=classpath:keys/app.key

# Definir o nível de log para toda a aplicação
logging.level.root=DEBUG

# Aumenta o nível de log para tomcat
logging.level.org.apache.coyote=DEBUG
logging.level.org.apache.tomcat=DEBUG
logging.level.org.apache.catalina=DEBUG

# Definir o nível de log para um pacote específico (ex: logs do Spring Security)
logging.level.org.springframework.security=DEBUG
# Definindo o perfil ativo baseado em variÃ¡vel de ambiente (padrÃ£o para 'dev')
spring.profiles.active=${APP_PROFILE:dev}

# ConfiguraÃ§Ã£o do CORS (ajustar conforme necessÃ¡rio)
cors.origins=${CORS_ORIGINS:http://localhost:5173,http://localhost:3000}

# Outras configuraÃ§Ãµes globais
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

spring.flyway.locations=classpath:db/migration